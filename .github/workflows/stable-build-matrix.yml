name: Stable Build Matrix

on:
  pull_request:
    branches:
      - stable
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-desktop:
    name: ${{ matrix.artifact_name }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            os_name: linux
            cargo_desktop_target: Builds/Desktop/Linux
            feature: gui
            artifact_name: arcadia-desktop-linux
          - os: ubuntu-latest
            os_name: linux
            cargo_desktop_target: Builds/Desktop/Linux
            feature: headless
            artifact_name: arcadia-desktop-linux-headless
          - os: macos-latest
            os_name: macos
            cargo_desktop_target: Builds/Desktop/OSX
            feature: gui
            artifact_name: arcadia-desktop-macos
          - os: macos-latest
            os_name: macos
            cargo_desktop_target: Builds/Desktop/OSX
            feature: headless
            artifact_name: arcadia-desktop-macos-headless
          - os: windows-latest
            os_name: windows
            cargo_desktop_target: Builds/Desktop/Windows
            feature: gui
            artifact_name: arcadia-desktop-windows
          - os: windows-latest
            os_name: windows
            cargo_desktop_target: Builds/Desktop/Windows
            feature: headless
            artifact_name: arcadia-desktop-windows-headless

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust dependencies
        uses: swatinem/rust-cache@v2
        with:
          workspaces: |
            Desktop -> ${{ matrix.cargo_desktop_target }}

      - name: Install Linux GUI build dependencies
        if: runner.os == 'Linux' && matrix.feature == 'gui'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libxcb1-dev \
            libxkbcommon-dev \
            libxkbcommon-x11-dev

      - name: Build Arcadia
        run: cargo build --manifest-path Desktop/Cargo.toml --target-dir ${{ matrix.cargo_desktop_target }} --no-default-features --features ${{ matrix.feature }}

      - name: Package binary (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path dist | Out-Null
          $target = "dist/${{ matrix.artifact_name }}.exe"
          Copy-Item "${{ matrix.cargo_desktop_target }}/debug/arcadia.exe" $target

      - name: Package binary (macOS/Linux)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          mkdir -p dist
          cp "${{ matrix.cargo_desktop_target }}/debug/arcadia" "dist/${{ matrix.artifact_name }}"

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: dist/*
          if-no-files-found: error

  build-mobile-ios:
    name: arcadia-mobile-ios
    runs-on: macos-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Build iOS app (simulator)
        shell: bash
        run: |
          xcodebuild \
            -project Mobile/iOS/ArcadiaApp.xcodeproj \
            -scheme ArcadiaApp \
            -configuration Release \
            -sdk iphonesimulator \
            -destination "generic/platform=iOS Simulator" \
            CODE_SIGNING_ALLOWED=NO \
            -derivedDataPath Builds/Mobile/iOS/DerivedData/Simulator \
            build

      - name: Package iOS app
        shell: bash
        run: |
          mkdir -p dist
          cp -R "Builds/Mobile/iOS/DerivedData/Simulator/Build/Products/Release-iphonesimulator/ArcadiaApp.app" "dist/arcadia-mobile-ios.app"

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: arcadia-mobile-ios
          path: dist/*
          if-no-files-found: error

  release:
    name: Create Stable Release
    needs: [build-desktop, build-mobile-ios]
    runs-on: ubuntu-latest
    steps:
      - name: Download matrix artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: arcadia-*
          path: release-assets
          merge-multiple: true

      - name: Compute build tag
        id: build_tag
        run: |
          echo "tag=build-${{ github.run_number }}" >> "${GITHUB_OUTPUT}"
          echo "name=Stable Build ${{ github.run_number }}" >> "${GITHUB_OUTPUT}"

      - name: Publish GitHub release with assets
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.build_tag.outputs.tag }}
          name: ${{ steps.build_tag.outputs.name }}
          target_commitish: ${{ github.sha }}
          generate_release_notes: true
          files: |
            release-assets/*
